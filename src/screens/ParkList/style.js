import { StyleSheet } from 'react-native';
import * as globals from '../../constants/global';

const styles = StyleSheet.create({
    content: {
        paddingTop: 20,
        backgroundColor: globals.GRAY_COLOR,
    },
});

export default styles;
